[package]
name = "hull-plugin-example"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Reference Hull plugin — shows the exact shape a plugin takes. A closed hosted plugin is structured identically; only the crate is private and the server registers it behind a different feature."

[dependencies]
hull-plugin = { path = "../../crates/hull-plugin" }
hull-scan = { path = "../../crates/hull-scan" }
